#include "main.h"
/**
* add - prints the result of two numbers
* @number1 : The first number
* @number2 : the second number
* Return: always 0
*
*/
int add(int number1, int number2)
{
return (number1 + number2);
}
