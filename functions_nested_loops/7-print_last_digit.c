#include "main.h"
/**
* last_digi- prints the last digit
* @n: The character to print
* Return: always 0
*
*/
int print_last_digit(int n)
{
int last_digit = n % 10;
if (last_digit >= 0)
return (last_digit);
}
