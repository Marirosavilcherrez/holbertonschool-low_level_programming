#include "main.h"
/**
* _isupper - prints the lowercase
* @c: The character to print
* Return: always 0
* Description:  Funtion to print the lowercase
*
*/
int _isupper(int c)
{

if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
